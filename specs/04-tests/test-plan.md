# Test Plan

## Scope

Automated tests are **excluded** from this project. The `fabric-integration` service is responsible for end-to-end testing with the deployed chaincode.

No unit test source directories, test dependencies, or test framework configuration should be added to this project.
